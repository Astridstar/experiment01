# This is a Databricks asset bundle definition for data_cleansing_experiment01.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: data_cleansing_experiment01
  uuid: 9a9d2a9b-175e-4f85-b3eb-588011282136

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-88571bfd-8566.cloud.databricks.com
    variables:
      catalog: dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-88571bfd-8566.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/pandawss@hotmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/pandawss@hotmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev
      schema: prod
    permissions:
      - user_name: pandawss@hotmail.com
        level: CAN_MANAGE
